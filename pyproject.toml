[project]
name = "mine-files"
version = "0.1.0"
description = "A sample Python utility library"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["mine_files"]

[tool.coverage.report]
show_missing = true
